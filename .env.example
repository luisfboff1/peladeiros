# Database (Neon Postgres)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# NextAuth / Auth.js (Authentication)
# NextAuth v5 usa AUTH_SECRET, mas também aceita NEXTAUTH_SECRET para compatibilidade
NEXTAUTH_URL=http://localhost:3000
AUTH_SECRET=your_secret_key_here_generate_with_openssl_rand_base64_32
# NEXTAUTH_SECRET=your_secret_key_here_generate_with_openssl_rand_base64_32  # Opcional (compatibilidade)
